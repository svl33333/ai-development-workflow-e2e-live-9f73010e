[C2C] operation=prototype_design
concept-briefと現行リポジトリをMCPで読み、prototype詳細設計・実装計画・評価基準を返してください。ファイル編集はしないでください。
## Issue #8 structured execution contract

Return prototype units with `hypothesis`, `success_criteria`, `failure_criteria`, and `evaluation_method` in addition to the common execution-manifest fields. The design must pass the existing independent review loop for at least three qualifying rounds, with findings and dispositions recorded before prototype execution approval.
